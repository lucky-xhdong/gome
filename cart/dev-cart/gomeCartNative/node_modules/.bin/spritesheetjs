#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../spritesheetjs/bin/spritesheetjs" "$@"
  ret=$?
else 
  node  "$basedir/../spritesheetjs/bin/spritesheetjs" "$@"
  ret=$?
fi
exit $ret
